# Journal lives in /run only. Trade-off: no `journalctl -b -1`, including the boot
# that failed before a rollback. Panics still reach /var/lib/systemd/pstore on
# hardware with a pstore backend.
[Journal]
Storage=volatile
RuntimeMaxUse=256M
